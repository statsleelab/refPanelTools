% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/read_region.R
\name{read_region}
\alias{read_region}
\title{Extract Genomic Region Data as a Data Frame}
\usage{
read_region(
  chr_num,
  start_bp,
  end_bp,
  index_data_file,
  reference_data_file,
  pop_desc_file = NULL,
  num_pops = NULL
)
}
\arguments{
\item{chr_num}{Integer. Chromosome number (1--22).}

\item{start_bp}{Integer. Start base pair position (inclusive).}

\item{end_bp}{Integer. End base pair position (inclusive).}

\item{index_data_file}{Character. Path to the BGZF-compressed reference
panel index file, whose columns are \verb{rsid chr bp a1 a2 af1ref fpos}.}

\item{reference_data_file}{Character. Path to the BGZF-compressed reference
genotype file.}

\item{pop_desc_file}{Character. Path to the population description file
(columns: \verb{pop_abb num_subj sup_pop_abb}, with a header row). Used to
label genotype and AF1 columns. If \code{NULL}, \code{num_pops} must be supplied
and generic names (\code{geno_pop1}, \code{af1_pop1}, ...) are used.}

\item{num_pops}{Integer. Total number of populations in the reference panel.
Required when \code{pop_desc_file} is \code{NULL}; ignored otherwise.}
}
\value{
A data.frame with one row per SNP in the region and columns:
\describe{
\item{rsid}{SNP identifier.}
\item{chr}{Chromosome number.}
\item{bp}{Base pair position.}
\item{a1, a2}{Reference and alternate alleles.}
\item{af1ref}{Overall reference panel AF1.}
\item{geno_\if{html}{\out{<pop>}}}{Character. Genotype string for each population (0/1/2
per subject, packed as a single string; leading zeros are preserved).}
\item{af1_\if{html}{\out{<pop>}}}{Numeric. Alternate allele frequency for each population.}
}
A region holding no SNPs gives a data.frame with zero rows and these same
columns, so results from several regions can be combined with \code{rbind()}
without special-casing the empty result.
}
\description{
Returns the SNPs in a base-pair range as a data.frame, combining metadata
from the index file with genotype strings and per-population allele
frequencies.
}
\details{
Metadata and genotype records are collected together in a single pass over
the index, so the cost is proportional to the size of the region rather than
the size of the index.
}
\examples{
\dontrun{
df <- read_region(
  chr_num             = 14,
  start_bp            = 104000000,
  end_bp              = 104200000,
  index_data_file     = "/33KG/33kg_index.gz",
  reference_data_file = "/33KG/33kg_geno.gz",
  pop_desc_file       = "/33KG/33kg_pop_desc.txt"
)
head(df[, c("rsid", "chr", "bp", "a1", "a2", "af1ref")])
}
}
\seealso{
\code{\link[=extract_reg_data]{extract_reg_data()}} to write the genotype records to a file
instead, which is the better choice for a region too large to hold in
memory.
}
