% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/read_region.R
\name{read_region}
\alias{read_region}
\title{Extract Genomic Region Data as a Data Frame}
\usage{
read_region(
  chr_num,
  start_bp,
  end_bp,
  index_data_file,
  reference_data_file,
  pop_desc_file = NULL,
  num_pops = NULL
)
}
\arguments{
\item{chr_num}{Integer. Chromosome number (1--22).}

\item{start_bp}{Integer. Start base pair position (inclusive).}

\item{end_bp}{Integer. End base pair position (inclusive).}

\item{index_data_file}{Character. Path to the reference panel index file.}

\item{reference_data_file}{Character. Path to the BGZF-compressed reference
genotype file.}

\item{pop_desc_file}{Character. Path to the population description file
(columns: \verb{pop_abb num_subj sup_pop_abb}, with a header row). Used to
label genotype and AF1 columns. If \code{NULL}, \code{num_pops} must be supplied
and generic names (\code{geno_pop1}, \code{af1_pop1}, ...) are used.}

\item{num_pops}{Integer. Total number of populations in the reference panel.
Required when \code{pop_desc_file} is \code{NULL}; ignored otherwise.}
}
\value{
A data.frame with one row per SNP in the region and columns:
\describe{
\item{rsid}{SNP identifier.}
\item{chr}{Chromosome number.}
\item{bp}{Base pair position.}
\item{a1, a2}{Reference and alternate alleles.}
\item{af1ref}{Overall reference panel AF1.}
\item{geno_\if{html}{\out{<pop>}}}{Genotype string for each population (0/1/2 per subject,
packed as a single character string).}
\item{af1_\if{html}{\out{<pop>}}}{Allele frequency of alternate allele for each population.}
}
Returns an empty data.frame if no SNPs fall in the requested region.
}
\description{
A convenience wrapper around \code{\link[=extract_reg_data]{extract_reg_data()}} that returns the extracted
data as a data.frame, combining SNP metadata from the index file with
genotype strings and per-population allele frequencies.
}
\details{
The raw output of \code{\link[=extract_reg_data]{extract_reg_data()}} contains one row per SNP with
space-separated genotype strings (one per population) followed by
per-population AF1 values. This wrapper parses that output and attaches
SNP metadata (rsid, chr, bp, a1, a2) from the index file.
}
\examples{
\dontrun{
df <- read_region(
  chr_num             = 14,
  start_bp            = 104000000,
  end_bp              = 104200000,
  index_data_file     = "/33KG/33kg_index.gz",
  reference_data_file = "/33KG/33kg_geno.gz",
  pop_desc_file       = "/33KG/33kg_pop_desc.txt"
)
head(df[, c("rsid", "chr", "bp", "a1", "a2", "af1ref")])
}
}
\seealso{
\code{\link[=extract_reg_data]{extract_reg_data()}} for the underlying file-based function.
}
